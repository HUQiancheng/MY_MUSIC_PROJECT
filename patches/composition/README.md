# Composition Patches

This folder hosts full-track composition trees.

Use the scaffold command to create a role-oriented structure for a track:

`node scripts/idm/scaffold-patch-tree.js --name "fractured-meridian"`
