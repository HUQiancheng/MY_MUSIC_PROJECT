# Sequencing Patches

Use this folder for rhythm and pattern systems (`seq-*.pd`), including motif banks, switch logic, and micro-timing controls.

Suggested flow:
- Separate motif generation from arrangement rules.
- Keep one patch per sequencing concern.
- Route re-usable logic to `/abstractions/rhythm`.
