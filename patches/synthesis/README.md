# Synthesis Patches

Use this folder for role-local synth voices (`syn-*.pd`) before they are promoted into reusable abstractions.

Suggested flow:
- Build raw timbre voices here.
- Validate CPU and dynamic range.
- Move stable units into `/abstractions/synth`.
